<div class="d-flex flex-column align-items-center vh-100 mt-4">
    <%= render "new" %>
    <div class="row col-6" style="width: 600px;">
        <% @topics.each do |item| %>
            <%= render "topic", item: item %>
        <% end %>
    </div>
</div>